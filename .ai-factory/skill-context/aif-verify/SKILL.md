# Project Rules for /aif-verify

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-05-25 23:00
> Based on: 9 analyzed patches

## Rules

### Architecture Doc Alignment Checks
**Source**: 2026-05-24-22.00, 2026-05-25-01.25, 2026-05-25-00.30
**Rule**: After implementation, verify: (1) loader `priority()` return values match the priority table in `ARCHITECTURE.md`; (2) `AGENTS.md` and `ARCHITECTURE.md` file tree diagrams reflect the actual `src/` structure; (3) `final` keyword convention is maintained on all concrete classes in `src/` (checked by `ArchitectureTest`). Run `grep -rn 'priority()' src/Loaders/` and compare against the ARCHITECTURE.md table.

### API and Namespace Consistency After Changes
**Source**: 2026-05-25-01.25, 2026-05-25-14.56
**Rule**: After namespace refactoring, run `grep -r "OldNamespace"` across the entire repository to verify no stale references remain — check `src/`, `tests/`, `config/`, `docs/`, `.ai-factory/`, and `AGENTS.md`. After breaking API changes (method renames, removed aliases, enum case renames), grep `README.md`, `docs/`, `AGENTS.md`, `ARCHITECTURE.md`, and `DESCRIPTION.md` for old method names, legacy manifest forms, and outdated contract descriptions.
