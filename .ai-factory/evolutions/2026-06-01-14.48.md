# Evolution: 2026-06-01 14:48

## Intelligence Summary

- Patches analyzed: 5 processed this run (3 new + 2 overlap), 22 total in project patch set
- New patches: `2026-05-29-17.20.md`, `2026-05-29-18.30.md`, `2026-06-01-14.39.md`
- Overlap patches: `2026-05-27-02.45.md`, `2026-05-27-03.09.md`
- Recurring patterns:
  - External JSON/state parsing must be closed-schema and distinguish missing keys from explicit `null`
  - Source provenance must describe the current installed bytes, not preserve stale sibling fields
  - Cross-field invariants should be encoded in VOs/enums, not factory convention only
  - Runtime narrowing must be swept through docs and AI context
  - Fix flows need mutating `composer rector` then `composer format`; review flows use dry-run checks
- Tech stack: PHP 8.3+, Laravel 12/13 package, Pest/PHPUnit, PHPStan/Larastan, Rector, PHP-CS-Fixer

## Improvements Applied

### aif-fix -> skill-context

- Added package-core override forbidding `[FIX]` runtime logging in `src/`; use typed exceptions, command output, and tests instead.
  **File:** `.ai-factory/skill-context/aif-fix/SKILL.md`
- Added post-fix command rule: run `composer rector` then `composer format`; dry-run commands are not substitutes in fix flow.
  **File:** `.ai-factory/skill-context/aif-fix/SKILL.md`
- Added strict external JSON parsing rule for closed keys and `array_key_exists()`.
  **File:** `.ai-factory/skill-context/aif-fix/SKILL.md`

### aif-implement -> skill-context

- Added whole-object source provenance rewrite and enum/VO invariant rules.
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Added fail-fast boundary validation before filesystem mutation.
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Added closed external JSON contract and explicit-null parsing rules.
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Added config-contract-consumer rule.
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Added PHP fix quality command order.
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`

### aif-review -> skill-context

- Added source provenance/state parsing checklist.
  **File:** `.ai-factory/skill-context/aif-review/SKILL.md`
- Added review-only quality command rule: `composer format:dry` and `composer rector:dry`.
  **File:** `.ai-factory/skill-context/aif-review/SKILL.md`

### aif-verify -> skill-context

- Added audit for post-fix `composer rector` before `composer format`.
  **File:** `.ai-factory/skill-context/aif-verify/SKILL.md`
- Added runtime context drift sweep for zip-only sources, checksum format, and config key paths.
  **File:** `.ai-factory/skill-context/aif-verify/SKILL.md`

### aif-docs -> skill-context

- Added runtime narrowing documentation sweep for zip-only install/update source and persisted format examples.
  **File:** `.ai-factory/skill-context/aif-docs/SKILL.md`

### aif-plan -> skill-context

- Added planning rule for provenance, JSON contracts, explicit null tests, and docs/context sweeps.
  **File:** `.ai-factory/skill-context/aif-plan/SKILL.md`
- Added final quality task planning rule: `composer rector` then `composer format`.
  **File:** `.ai-factory/skill-context/aif-plan/SKILL.md`

### aif-qa -> skill-context

- Added state parsing scenarios and deterministic cleanup-test guidance.
  **File:** `.ai-factory/skill-context/aif-qa/SKILL.md`

### aif-commit -> skill-context

- Added pre-commit reminder for PHP bugfix quality command order.
  **File:** `.ai-factory/skill-context/aif-commit/SKILL.md`

### aif-evolve -> skill-context

- Added evolve-specific harmful-context audit rule and exact command-order preservation rule.
  **File:** `.ai-factory/skill-context/aif-evolve/SKILL.md`

## Harmful or Stale Context Findings

- `.ai-factory/rules/runtime.md` contains a likely harmful statement: `FeatureRepositoryInterface` should read values from current `module.json`. Current runtime and docs say feature values are read from `state.json` through `ModuleStateRepositoryInterface`.
  - Not modified by `$aif-evolve` because `.ai-factory/rules/*` is owned by `$aif-rules`.
  - Suggested remediation: `$aif-rules` should replace `module.json` with `state.json` in that runtime rule.
- `.ai-factory/ROADMAP.md` still describes local quality gates with `composer rector:dry` / `composer format:dry`; this may be acceptable for review/CI-readiness context, but it should not override fix/implementation flow rules added above.
- Historical plan artifacts still contain stale directory-source language. They were treated as archived planning context, not current runtime documentation.

## Patterns Identified

- Provenance consistency: 2 new patches
- Closed state parsing / missing-vs-null: 1 new patch
- Docs and AI-context drift after runtime narrowing: 2 new patches
- Deterministic tests over wall-clock assertions: 1 new patch
- Post-fix mutating quality command order: user directive
